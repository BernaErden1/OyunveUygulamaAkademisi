import 'package:flutter/material.dart';

class ProfilAyarlarSayfasi extends StatelessWidget {
  const ProfilAyarlarSayfasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
